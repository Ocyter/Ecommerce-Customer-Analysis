const glow=document.querySelector('.cursor-glow');if(glow&&matchMedia('(pointer:fine)').matches)addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px'});const progress=document.getElementById('scrollProgress');addEventListener('scroll',()=>{if(progress){const m=document.documentElement.scrollHeight-innerHeight;progress.style.width=(scrollY/m*100)+'%'}});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));document.querySelectorAll('.filter').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');const f=b.dataset.filter;document.querySelectorAll('[data-tags]').forEach(c=>c.style.display=f==='all'||c.dataset.tags.split(' ').includes(f)?'':'none')});